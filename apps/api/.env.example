# 复制此文件为 .env，API 启动时会自动加载
# cp apps/api/.env.example apps/api/.env

# API Server
PORT=3000
FRONTEND_URL=http://localhost:5173

# PostgreSQL persistent cache
# 根据本地 PostgreSQL 配置调整用户名和密码
DATABASE_URL=postgresql://localhost:5432/kok_cache
